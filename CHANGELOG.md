# 变更记录

本项目遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [0.5.1] - 2026-08-14

### 修复

- 更正了 README.md 中安装方式的瑕疵。

## [0.5.0] - 2026-08-14

### 新增

- 设置面板新增「回到上一句」小按钮（左箭头，无文字）：可回退最近 5 次刷新，防止手快错过想看的句子。
- 随机选句近 10 步去重：最近显示过的 10 句不会被随机换出（每日复用与手动回退不受影响）。
- 随机算法重写为无偏的候选池采样：排除集不影响剩余候选项的均匀分布，全部被排除时退化为全池随机，永不失败。
- 测试新增：缓冲区上限、排除集映射、采样均匀性（2000 次分布检验）、语料与 txt 全量一致性、22 条出处人工抽查核对。

### 修复

- 侧边栏收起成窄轨时自动关闭设置面板（其定位锚点已随显示块消失）。
- 三处引文/出处更忠实于原文：《情人》摘句、汪曾祺句补足原文、丰子恺补篇名。

## [0.4.0] - 2026-08-14

### 新增

- 语料库扩充至 100 余条：新增文学作品句子 31 条（张爱玲、沈从文、史铁生、王小波、木心、老舍、汪曾祺、北岛、舒婷、三毛、金庸、路遥、莫言、海明威、加缪、马尔克斯、博尔赫斯、王尔德、村上春树等），古诗词占比降至约三分之一。

## [0.3.1] - 2026-08-14

### 修复

- 设置面板下沿始终锚定显示块上沿：换句导致显示块（及面板内预览）高度变化时，面板通过 `ResizeObserver` 实时跟随重定位，不再遮蔽或远离显示区域；窗口缩放时同样重新定位而非关闭面板。

## [0.3.0] - 2026-08-14

### 新增

- 语料库外置：项目根目录新增 `corpus.txt`，每行一条 `句子 | 出处`，可直接编辑增删。
- 宿主半新增路由 `/plugins/dsh-moyan/corpus.txt`，每次请求实时读取语料文件；改完 txt 刷新页面即生效，**无需重启 dsh web**。
- 浏览器半优先加载 txt 语料；文件缺失/读取失败时回退到内置语料，插件始终可用。
- 测试新增：语料解析、txt 与内置语料一致性、自定义语料选句、宿主路由行为。

### 修复

- 设置面板中「每次打开」与「每天」按钮改为等宽（`flex: 1 1 0`）。

## [0.2.0] - 2026-08-14

### 变更

- 插件更名为「墨言」（包名 `dsh-moyan`，loader 行 id `moyan`，localStorage 键 `dsh-moyan:v1`）。

### 新增

- 显示区域改为多行完整显示：句子按需折行（1-3 行）完整展示，出处单独一行右对齐，**不再出现省略号**。
- 设置按钮移至出处行尾，悬停整块时淡入，不再遮挡正文。

## [0.1.0] - 2026-08-14

### 新增

- 在 WebUI 侧边栏「设置」上方显示一句名言/诗词/游戏台词，随主题切换明暗配色。
- 刷新频率设置：每次打开 WebUI 刷新、或每天刷新（跨午夜自动换句）。
- 悬停显示设置按钮，弹出小面板：切换刷新频率、手动「换一句」、查看完整出处。
- 内置 70 余句语料（古诗词、名著名句、游戏/动漫台词），单句不超过 30 字。
- 设置持久化于浏览器 localStorage。
- 自带无需依赖的单元测试（`npm test`），覆盖选句逻辑与注册行为。
